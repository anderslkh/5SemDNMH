@using Models
@model List<Gallery>

@{
    ViewData["Title"] = "Galleries";
}

<h1>Galleries</h1>



<hr>

<div>
    <h4>Gallery</h4>
    <hr />
    @if (true)
    {
        @foreach (var item in Model)
        {
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => item.GalleryName)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => item.GalleryName)
            </dd>
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => item.Id)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => item.Id)
            </dd>
            <dt class="col-sm-2"></dt>
            <dd class="col-sm-10">
                @Html.ActionLink("Show Gallery", "Gallery", new { imageIds = string.Join(",", item.ImageIds) })
            </dd>
        </dl>
        }
    }
</div>

<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
